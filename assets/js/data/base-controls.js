// note this just contains placeholders pending a decision on what minimum
// wp version requirements will exist for js data stores in the plugin.

export function apiFetch( request ) {}

export function select( storeKey, selectorName, ...args ) {}

export function dispatch( storeKey, actionName, ...args ) {}
